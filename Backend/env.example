# Path to the Firebase service account JSON key (Project Settings > Service accounts > Generate new private key).
# Never commit the actual key file - it is gitignored as serviceAccountKey.json.
GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json

# Comma-separated list of origins allowed to call this API (the Vite dev server by default).
CORS_ORIGINS=http://localhost:5173
